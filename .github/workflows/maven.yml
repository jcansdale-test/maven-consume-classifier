on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - run: mvn clean package -s settings.xml --no-transfer-progress
      env:
        GITHUB_TOKEN: ${{ github.token }}
